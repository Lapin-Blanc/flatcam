# Marker so the bundled resource images (icons, cursors) ship as package data
# and install alongside the modules. FlatCAMApp chdir()s to its own directory
# and loads icons via relative "share/<file>" paths.
