# This file contains python only requirements to be installed with pip
# Usage: pip install -r requirements.txt
# Ported to Python 3.x / PySide6 / shapely 2.x
# Ranges kept in sync with pyproject.toml [project.dependencies].
PySide6>=6.5,<7
numpy>=2.0,<3
matplotlib>=3.5,<4
shapely>=2.0,<3
simplejson>=3.17,<5
rtree>=1.0,<2
svg.path>=7.0,<8
